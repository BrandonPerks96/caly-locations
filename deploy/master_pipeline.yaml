# Python to Linux Web App on Azure
# Build your Python project and deploy it to Azure as a Linux Web App.
# Change python version to one thats appropriate for your application.
# https://docs.microsoft.com/azure/devops/pipelines/languages/python

trigger:
- main

variables:
  # Azure Resource Manager connection created during pipeline creation
  - group: caly_loc_vars

stages:
- stage: Build
  displayName: Build stage
  jobs:
  - job: BuildJob
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: UsePythonVersion@0
      inputs:
        versionSpec: '$(pythonVersion)'
      displayName: 'Use Python $(pythonVersion)'

    - script: |
        echo "Upgrading pip"
        python -m pip install --upgrade pip
        echo "Installing requirements"
        python -m pip install -r requirements.txt
      workingDirectory: $(projectRoot)
      displayName: "Install requirements"

    - task: ArchiveFiles@2
      displayName: 'Archive files'
      inputs:
        rootFolderOrFile: '$(projectRoot)'
        includeRootFolder: false
        archiveType: zip
        archiveFile: $(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip
        replaceExistingArchive: true
        displayName: 'Caly Locations artifact'

    - upload: $(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip
      displayName: 'Upload package'
      artifact: drop

- stage: DeployNonProd
  displayName: 'Deploy to Non-Production'
  dependsOn: Build
  condition: succeeded()
  jobs:
  - deployment: DeploymentJobNonProd
    pool:
      vmImage: $(vmImageName)
    environment: $(nonprodEnvironmentName)
    strategy:
      runOnce:
        deploy:
          steps:
          - task: UsePythonVersion@0
            inputs:
              versionSpec: '$(pythonVersion)'
            displayName: 'Use Python version'

          - task: AzureWebApp@1
            displayName: 'Deploy Azure Web App (Non-Prod)'
            inputs:
              azureSubscription: $(azureServiceConnectionId)
              appName: $(nonprodAppName)
              package: $(Pipeline.Workspace)/drop/$(Build.BuildId).zip

# - stage: DeployProd
#   displayName: 'Deploy to Production'
#   dependsOn: DeployNonProd
#   condition: succeeded()
#   jobs:
#   - deployment: DeploymentJobProd
#     pool:
#       vmImage: $(vmImageName)
#     environment: $(prodEnvironmentName)
#     strategy:
#       runOnce:
#         deploy:
#           steps:
#           - task: UsePythonVersion@0
#             inputs:
#               versionSpec: '$(pythonVersion)'
#             displayName: 'Use Python version'

#           - task: AzureWebApp@1
#             displayName: 'Deploy Azure Web App (Prod)'
#             inputs:
#               azureSubscription: $(azureServiceConnectionId)
#               appName: $(prodAppName)
#               package: $(Pipeline.Workspace)/drop/$(Build.BuildId).zip